import axios from '@/utils/axios'

const LoginModel = {
    // 定义共享变量数据
    state: {

    },

    // 获取和存放修改state的方法
    selectors: (state) => {
        const selectors = {
            // getAvatarImg: () => state.AvatarImg
        }
        return selectors
    },

    // 接口apis
    apis: {

    },

    // actions
    actions: {

    },

    // reducer
    reducers: {

    }

}

export default LoginModel